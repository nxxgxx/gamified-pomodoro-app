// Pomodoro timer initial values
export const DEFAULT_FOCUS_SEC = 25 * 60;
export const DEFAULT_BREAK_SEC = 5 * 60;
export const DEFAULT_REST_SEC = 15 * 60;

// Modes
export const MODES = {
    FOCUS: 'focus',
    BREAK: 'break',
    REST: 'rest'
}
